package Functions.Modifiers;

import Functions.Value;

/*
 * Stores two values and does something with those two values in order to create a new value.
 * Examples of this would be Add(a, b) which evaluates to a.getValue + b.getValue, or Multiply, which would do
 * exactly the same.
 */
public interface Modifier extends Value {

}
